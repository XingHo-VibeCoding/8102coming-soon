# 「中V歌曲故事」技术设计文档（TECH_DESIGN.md）

> 版本：v1.0 ｜ 日期：2026-09-24 ｜ 依据：PRD.md v1.0 + research.md
> 读者：未来的我自己（以及帮我写代码的 AI）

---

## 1. 技术方案比较与推荐

### 方案 A：纯前端静态站（数据写死在 JS 文件，GitHub Pages 托管）

### 方案 B：前端 + 云后端（云数据库 + API，数据存云端在线改）

| 维度 | 方案 A：纯静态 | 方案 B：云后端 | 说明 |
|---|---|---|---|
| **学习成本** | ⭐ 低：HTML/CSS/JS 基础即可，无后端概念 | ⭐⭐⭐ 高：数据库、API、鉴权、异步请求全要学 | 我是零基础起步，此项权重最高 |
| **线上持久化** | ⚠️ 够用但有限：数据在代码文件里，改数据=改文件+git push | ✅ 真·在线增删改，手机上也能改 | 我的场景是"低频录入、频繁阅读"，A 的改数据方式可接受 |
| **费用** | ✅ 0 元（GitHub Pages 免费额度远够） | ⚠️ 免费额度小心超（流量/行数限制），超了要绑卡 | 学生项目，0 元是硬指标 |
| **排错难度** | ⭐ 低：出问题就三层——代码、数据、浏览器控制台，无中间环节 | ⭐⭐⭐ 高：前端、网络、云端、权限任何一层都能坏，账号欠费也会"突然打不开" | 独自排错，层数越少越好 |
| **部署** | push 即上线 | 要配环境、密钥、部署流程 | — |

### ✅ 推荐：方案 A（纯静态）

**理由（按我的限制逐条对应）**：

1. **电脑限制**：纯静态不需要装任何新东西——Git 和 Node.js 都已就绪，普通电脑跑编辑器+浏览器毫无压力；方案 B 还要跑本地后端联调或依赖云控制台
2. **时间限制**：一周 MVP 里，省下的后端时间全部投给分镜脚本和配图——那才是这个项目"故事完整呈现"的核心价值
3. **账号限制**：只用已有的 GitHub，不注册任何新平台、不绑卡
4. **需求匹配**：PRD 的数据对象总共 4 个、首批数据量是个位数歌曲，"在线改数据"的需求在 MVP 阶段不存在

**何时升级到方案 B（附录判断条件，本期不做）**：歌曲超过约 50 首 / 想在手机上直接录入分镜 / 开放给朋友投稿。到时数据层按第 6 节的迁移路径切换，页面代码可以不大动。

---

## 2. 项目结构

```
世末歌者/                        ← 仓库根目录（已是 Git 仓库）
├── index.html                  ← 入口 + 首页（歌曲卡片列表 + 搜索 + 系列入口）
├── css/
│   └── style.css               ← 全站样式（深蓝"末日夜"色调、画格、动效、移动端）
├── js/
│   ├── data.js                 ← 【唯一的数据文件】歌曲/系列/分镜/里程碑全在这
│   ├── store.js                ← 数据访问层：读取、搜索、筛选（页面只调它，不直接碰 data.js）
│   ├── render.js               ← 渲染：把数据画成卡片/漫剧页/系列页
│   └── app.js                  ← 路由与交互：hash 路由、搜索框事件、滚动动效
├── assets/
│   └── panels/                 ← 画格配图（AI 生成原创插画，命名如 shimo-p1.jpg）
├── 404.html                    ← GitHub Pages 自定义 404
├── PRD.md / research.md / TECH_DESIGN.md / AGENTS.md
└── .gitignore                  ← 已含 .env 等保护规则
```

**结构原则**：页面之间不跳转 html 文件，而是单页 + `#/song/xxx` 式 hash 路由（刷新/分享链接都能正确定位，且不依赖服务器路由）。

---

## 3. 数据对象及字段（写在 js/data.js）

四个对象，全部是普通 JS 对象/数组，不依赖任何数据库。

### 3.1 Series（系列曲）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | string | ✅ | 唯一标识，英文（如 `shimo`） |
| name | string | ✅ | 系列名（如"世末歌者系列"） |
| intro | string | ✅ | 一句话世界观简介 |
| songIds | string[] | ✅ | 收录歌曲的 id 列表 |

### 3.2 Song（歌曲条目）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | string | ✅ | 唯一标识（如 `shimo-song`） |
| title | string | ✅ | 曲名 |
| singer | string | ✅ | 虚拟歌手（筛选维度①） |
| author | string | ✅ | UP主/作者 |
| date | string | ✅ | 投稿日期 `YYYY-MM-DD` |
| bilibili | string | ✅ | B站链接（外跳用，不内嵌） |
| badges | string[] | ✅ | 称号徽章（如 `["神话曲","最慢神话"]`，筛选维度②） |
| intro | string | ✅ | 一句话简介（首页卡片用） |
| seriesId | string | ⬜ | 所属系列，无系列可不填 |
| panels | Panel[] | ✅ | 分镜列表，4–6 个（核心字段，见 3.3） |
| milestones | Milestone[] | ✅ | 播放里程碑（见 3.4） |
| myStory | string | ✅ | 💙 我的故事（私人感想） |

### 3.3 Panel（分镜/画格）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| image | string | ⬜ | 配图路径（`assets/panels/...`）；**不填则渲染为色块+旁白的降级样式** |
| caption | string | ✅ | 一句旁白 |
| note | string | ⬜ | 该格的备注（可选） |

### 3.4 Milestone（播放里程碑）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| label | string | ✅ | 节点名（如"100万播放"、"神话曲"） |
| date | string | ✅ | 达成日期 `YYYY-MM-DD` 或 `YYYY` |

---

## 4. API 列表

**没有后端，因此没有 HTTP API。** 这里的"API"指数据访问层（js/store.js）暴露给页面的函数——将来迁移方案 B 时，只需把这 5 个函数的内部实现换成 fetch 云端请求，页面代码零改动：

| 函数 | 输入 | 输出 | 服务于 PRD |
|---|---|---|---|
| `getAllSongs()` | — | Song[] | F1 首页卡片列表 |
| `getSongById(id)` | 歌曲id | Song 或 null | F3 漫剧页 |
| `getAllSeries()` | — | Series[]（含每系列歌曲数） | F2 系列列表 |
| `getSeriesById(id)` | 系列id | Series + Song[] 或 null | F2 系列详情页 |
| `searchSongs(keyword)` | 关键词 | Song[]（匹配曲名/歌手/系列名，大小写不敏感） | F4 搜索 |

**外部依赖**：仅一个——B站视频外链（`window.open` 新标签页）。不调用任何需要密钥的接口。

---

## 5. 数据流

```
用户操作                页面层(render/app)          数据层(store)         数据源(data.js)
────────              ─────────────────          ─────────────        ─────────────
打开首页      ──────→  调 getAllSongs()   ──────→  读内存数组    ──────→  SONGS / SERIES
输入搜索词    ──────→  调 searchSongs(kw)  ──────→  过滤+返回    （已在内存，无网络请求）
点卡片        ──────→  路由变 #/song/id   ──────→  getSongById
滚动漫剧页    ──────→  画格进入视口 → CSS 渐显动效（IntersectionObserver）
点"去B站听"   ──────→  window.open(bilibili 链接)
```

- **数据只有一条来源**：data.js。加载时一次性读入内存，之后所有搜索/筛选都是内存操作（十几条数据毫秒级），无任何运行时网络数据请求
- **改数据的唯一方式**：编辑 data.js → git push → GitHub Pages 自动更新（配合 .gitignore 保护，数据里永远不出现密钥）

---

## 6. 错误处理

| 场景 | 处理方式 | 用户看到 |
|---|---|---|
| 链接指向不存在的歌曲/系列（如手输错 hash） | `getSongById` 返回 null → 路由兜底 | 友好的"没有找到这个页面"+ 返回首页按钮 |
| 搜索无结果 | 空状态分支 | "没有找到相关歌曲"+ 清空搜索按钮，不清屏不报错 |
| 画格配图加载失败 | `onerror` 降级 | 该格变"色块+旁白"排版，故事依然完整可读 |
| 数据文件字段缺失（录入手误） | store.js 读取时校验，缺必填字段在控制台 `console.warn` 并跳过该条 | 页面不白屏，最多少一张卡片 |
| 404（直接访问不存在路径） | GitHub Pages 指向 404.html | 自定义 404 页 + 返回首页 |
| JS 加载失败/浏览器过旧 | `<noscript>` + 关键内容尽量服务静态 HTML | 提示"需要启用 JavaScript" |

**原则**：任何单点失败（一张图、一条数据）都不允许整页崩溃——故事的呈现优先级最高。

---

## 7. 环境变量

**方案 A 不需要任何环境变量**——无后端、无密钥、无第三方服务。

- `.gitignore` 里的 `.env` 保护规则**继续保留**：它是给"以后"准备的（PRD 提到第 23 天数据库配置），现在就养成密钥不进仓库的习惯
- 若未来升级方案 B：`.env` 将存放数据库连接串/密钥，**只存本地，永不提交**（届时升级 .gitignore 确认覆盖 `.env.*`）

---

## 8. 部署与迁移注意事项

### 8.1 部署（GitHub Pages，一次性配置）

1. 仓库 Settings → Pages → Source 选 **Deploy from a branch** → 分支 `main` / 目录 `/ (root)`
2. 约两分钟后，`https://xingho-vibecoding.github.io/8102coming-soon/` 生效；之后**每次 push 自动更新**（约 1 分钟）
3. **坑位清单**：
   - 路径大小写必须与文件名完全一致（Pages 服务器区分大小写，本地 Windows 不区分——本地能开、线上 404 的头号原因）
   - 画格图片文件名**只用英文小写+数字**（如 `shimo-p1.jpg`），避免中文名在线上出编码问题
   - 图片压到每张 200KB 以内（首页+漫剧页 3 秒可交互的非功能需求）

### 8.2 迁移（A → B 的预留路径，本期不做但留好门）

- **数据**：data.js 的 4 个对象结构即 JSON 结构——迁移时把 `SONGS`/`SERIES` 数组原样导入云端数据库即可，字段不用改
- **代码**：页面只调 store.js 的 5 个函数——迁移时只改这 5 个函数内部为云端请求（改成 async），render 层不动
- **代价预告**：届时所有调用点要补 async/await、要处理网络失败重试、要管密钥——这正是今天选 A 的原因

---

## 9. 参考

- 需求来源：PRD.md（功能 F1–F5、非功能需求、验收 A1–F4）
- 数据字段依据：research.md 7.1《世末歌者》字段示例
